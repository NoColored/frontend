import { style } from '@vanilla-extract/css';

export const exampleStyle = style({
  color: 'red',
  fontSize: 20,
  fontWeight: 'bold',
});

export const testStyle = style({
  color: 'blue',
  fontSize: 48,
});
