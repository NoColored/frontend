import * as rankConstants from '@/components/imagebox/TierBox/constants';

interface RankingItemBoxProps {
  rank: number;
  imgSrc: string;
  label: string;
  nickname: string;
  tier: ;
  score: number;
}
